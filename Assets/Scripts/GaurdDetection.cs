using System.Collections;
using System.Collections.Generic;
using TMPro;
using Unity.VisualScripting;
using UnityEngine;

public class GaurdDetection : MonoBehaviour
{
    /*
    private void Update()
    {
        float alignment = Vector2.Dot(vectorA, vectorB);

        float dotProduct = Vector3.Dot(transform.forward, (targetPosition - transform.position).normalized);

        if (dotProduct > 0.5f)
        {
            Debug.Log("Target is nearby!");
        }
    }

    public bool InFieldOfView(Transform target, float viewAngle)   // why view angle?
    {

    } */
}
